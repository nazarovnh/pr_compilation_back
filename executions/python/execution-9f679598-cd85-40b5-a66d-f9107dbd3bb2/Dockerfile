FROM python:3

WORKDIR /app
ARG EXECUTION_PATH
ARG SOURCE_CODE_FILE_NAME

ARG INPUT

ADD . .

RUN chmod a+x ${EXECUTION_PATH}/${SOURCE_CODE_FILE_NAME}

ENTRYPOINT ["/bin/sh", "-c", "python ${EXECUTION_PATH}/${SOURCE_CODE_FILE_NAME} ${INPUT}"]
